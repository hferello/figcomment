{
  "name": "figma-comment-intelligence-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Demo Figma plugin for AI-structured comment analysis.",
  "scripts": {
    "build": "esbuild code.ts --bundle --platform=browser --target=es2020 --format=iife --outfile=code.js",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2"
  }
}
